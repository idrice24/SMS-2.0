import { ForbittenFNameValidatorDirective } from './forbitten-f-name-validator.directive';

describe('ForbittenFNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbittenFNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
